# dotforge — macOS defaults seed
#
# Minimal example. Replace with your own state by running:
#     dot snapshot
# which overwrites this file with `scan-macos-defaults --capture` output
# from the current Mac, then commits to your private fork.
#
# Lines starting with `#` are ignored; everything else is fed to
# `bash -c` by .chezmoiscripts/run_onchange_60-apply-macos-defaults.sh.tmpl.

defaults write com.apple.dock autohide -bool true
defaults write com.apple.finder ShowPathbar -bool true
defaults write com.apple.finder ShowStatusBar -bool true
defaults write com.apple.screencapture location -string "$HOME/Pictures/Screenshots"
defaults write -g ApplePressAndHoldEnabled -bool false
